<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.piyush.magicalmethods">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddToDatabase"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegisterActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".Explore"
            android:configChanges="orientation|screenSize"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".Developers"
            android:configChanges="orientation|screenSize"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".HelpAndFeedback"
            android:configChanges="orientation|screenSize"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".CourseDetail"
            android:configChanges="orientation|screenSize"
            android:theme="@style/CustomTheme" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".VideoPlayerActivity"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PaymentWebviewActivity"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".PaymentResponseActivity"
            android:label="@string/title_activity_payment_response_activity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />

                <action android:name="android.intent.action.VIEW" />

                <data android:scheme="payment_response" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CourseDetailsNew"
            android:label="@string/title_activity_course_details_new"
            android:theme="@style/AppTheme.Appcompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>